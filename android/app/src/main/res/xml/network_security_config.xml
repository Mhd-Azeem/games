<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Only local assets — no network needed -->
    <domain-config cleartextTrafficPermitted="false">
        <domain includeSubdomains="false">localhost</domain>
    </domain-config>
</network-security-config>
